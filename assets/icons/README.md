# Header Wi-Fi icons

16 x 16 one-bit masks used by the Header status cluster. The committed
bitmaps live in `include/luma/assets/wifi-icons.h` and are painted at
runtime with Theme `primary_text` / `secondary_text`.
