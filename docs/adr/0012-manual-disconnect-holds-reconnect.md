# Manual Disconnect holds reconnect

A user Disconnect from the Wi-Fi Status section stops the radio and holds Network background reconnect until the next connect or reboot. Immediate or timed reconnect after a deliberate Disconnect would fight the user's choice; dropping the hold on the next connect or reboot keeps Saved profiles available without a hidden "stay offline" preference.
