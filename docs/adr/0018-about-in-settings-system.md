# About is a System nested view, not a Launcher App

Identity belongs next to the other device facts in Settings. A third Launcher App card only duplicated Settings → System → About. Confirm on About now fills the detail pane with Version, Model, and Repository instead of entering a full-screen App.
